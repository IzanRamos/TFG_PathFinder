<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permisos -->
    <!-- Permiso internet -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permisos de ubicación -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pathfinderlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logopathfinder_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PathFinder"
        tools:targetApi="31">
        <meta-data
            android:name="org.osmdroid.config"
            android:value="@string/osmdroid_config" />

        <activity
            android:name=".MainActivity"
            android:name=".fragments.ExplorarRutasFragment"
            android:exported="false" />
        <activity
            android:name=".fragments.MapaFragment"
            android:exported="false" />
        <activity
            android:name=".fragments.MisRutasFragment"
            android:exported="false" />
        <activity
            android:name=".NavigationActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>